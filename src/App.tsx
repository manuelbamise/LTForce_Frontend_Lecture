import NavBar from "./components/navBar";

const App = () => {
  // const age = 18;
  // const ageLimit = 18;

  // const calAge = (x: number, y: number): number => {
  //   return x + y;
  // };
  // condition ? true : false

  return (
    <>
      <NavBar />
    </>
  );
};

export default App;
